# 我要工作 — 大学生智能就业与互助成长平台 开发文档（优化版最终稿）

> 版本：v2.0（基于 v1.0 审批报告全部修订要点合并而成）
> 性质：可直接用于指导编码与毕设实施的最终基准文档
> 变更说明：本版相对 v1.0 落实了 6 项必要修订 + 3 项可选优化，变更点在各章节以 `[v2.0变更]` 标注

---

## 1. 项目概述

**项目名称**：我要工作（IWantJob）
**项目定位**：面向大学生及毕业 3 年内青年的 **AI 驱动就业 + 数据透明 + 职场模拟 + 互助成长** 一体化平台。

**核心目标**：以职位信息聚合和 AI 简历优化为基础，创新引入 **薪资数据众筹与实时白皮书**、**AI 职业模拟舱**、**防篡改可信徽章** 三大差异化模块，深度结合社区互助与求职帮帮团，形成"数据贡献—能力提升—价值锚定"的闭环生态。

**当前阶段**：后端核心功能实现，前后端分离，前端暂缓。

**核心创新点**：
- **薪资透明计划**：学生脱敏贡献 offer 薪资，AI 生成动态《大学生就业薪酬白皮书》，贡献者获得积分 + 精准职位匹配。
- **AI 职业模拟舱**：用文本分支剧情模拟入职、汇报、冲突等真实职场场景，AI 实时反馈，弥补招聘平台忽略的软技能训练。
- **防篡改可信徽章** `[v2.0变更]`：基于数据库约束 + 哈希指纹 + 审计日志实现业务层防伪与可追溯的技能背书徽章，平台自动铸造，构建可信成长档案。（原"不可篡改"措辞修正，避免答辩质疑）

## 2. 技术栈选型 `[v2.0变更：分级引入]`

| 类别 | 技术 | 分级 | 引入阶段 |
|------|------|------|----------|
| 后端框架 | Spring Boot 3.3.x + JDK 17 | 必选 | 第一阶段起 |
| 数据库 | MySQL 8.0 | 必选 | 第一阶段起 |
| ORM | MyBatis-Plus 3.5+ | 必选 | 第一阶段起 |
| 缓存/会话 | Redis 7.x | 必选 | 第一阶段起 |
| 文件存储 | MinIO | 必选 | 简历模块起 |
| 消息队列 | RabbitMQ 3.13 | 可选增强 | 薪资白皮书异步入库时引入；之前用 Spring `@Async` |
| 搜索引擎 | Elasticsearch 8.x | 可选增强 | 第二阶段/数据量达标后；之前用 MySQL FULLTEXT + Like 兜底 |
| 接口文档 | Knife4j (SpringDoc) | 必选 | 第一阶段起 |
| 认证鉴权 | Spring Security + JWT | 必选 | 第一阶段起 |
| 工具库 | Lombok, Hutool, MapStruct | 必选 | 第一阶段起 |

> **分级原则**：MVP 阶段仅需 MySQL + Redis，显著降低启动门槛，且不影响创新点落地。ES / RabbitMQ 在第七周作为增强引入。

## 3. 中间件本地部署 (docker-compose.yml)

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: iwantjob
    ports:
      - "3306:3306"
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  rabbitmq:          # [v2.0] 可选，第七周起启用
    image: rabbitmq:3.13-management
    ports:
      - "5672:5672"
      - "15672:15672"
  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
  elasticsearch:     # [v2.0] 可选，第七周起启用
    image: elasticsearch:8.12.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    ports:
      - "9200:9200"
```

## 4. 项目结构与模块依赖 `[v2.0变更：明确依赖方向]`

```
iwantjob-backend
├── iwantjob-common            // 公共类、异常、枚举、DTO、Result<T>、错误码
├── iwantjob-framework         // 全局配置（Security、Redis、MQ、Swagger、ES、审计、数据权限切面）
├── iwantjob-modules
│   ├── iwantjob-user          // 用户服务（注册、认证、个人信息、角色、积分）
│   ├── iwantjob-job           // 职位服务（聚合、发布、搜索、投递、内推）
│   ├── iwantjob-resume        // 简历服务（简历CRUD、AI分析、评分）
│   ├── iwantjob-interview     // 面试服务（模拟面试、题库、AI反馈）
│   ├── iwantjob-salary        // 薪资白皮书服务（数据脱敏贡献、审核、报告生成）
│   ├── iwantjob-simulator     // AI职业模拟舱服务（场景、节点图、分支剧情、反馈）
│   ├── iwantjob-badge         // 成就徽章服务（事件驱动铸造、哈希锁定、审计）
│   ├── iwantjob-community     // 社区服务（问答、帖子、技能交换、众筹）
│   └── iwantjob-helpgroup     // 求职帮帮团服务（失败求帮、互助任务匹配）
├── iwantjob-ai               // AI基础设施（仅封装千问调用、鉴权、重试、限流；不含业务编排）
└── iwantjob-api               // 聚合启动模块
```

**模块依赖规范（必须遵守）**：
```
iwantjob-api  ──依赖──> 各业务模块
各业务模块    ──依赖──> iwantjob-framework, iwantjob-common
iwantjob-ai   ──定位──> AI 基础设施（仅 chat(prompt,history) 原子能力）
业务模块间    ──禁止直接互调──> 通过 Spring 事件 / MQ 解耦
积分/用户     ──归属──> iwantjob-user（其他模块发事件通知积分变更，禁止跨模块直接写彼此表）
依赖方向      ──禁止──> ai 不得反向依赖业务模块
```

每个模块标准分层：`controller` → `service` → `mapper` → `entity` → `dto`

## 5. 数据库设计

### 5.1 表结构总览

**基础模块**
- 用户与认证：`sys_user`、`user_profile`、`user_auth`、`sys_audit_log` `[v2.0新增]`
- 职位与投递：`job`、`job_application`、`internal_referral`
- 简历与AI优化：`resume`、`resume_optimization_log`
- 模拟面试：`mock_interview`、`interview_question`、`question_bank`
- 社区互助：`post`、`answer`、`skill_exchange`、`crowdfunding_project`
- 求职帮帮团：`help_group_request`
- 积分与权益：`mutual_points`、`point_transaction`、`unlock_record`
- AI会话：`ai_conversation`、`ai_message`

**创新模块**
- 薪资白皮书：`salary_report_data`、`salary_whitepaper`、`salary_contribution_reward`、`salary_review_log` `[v2.0新增]`
- AI职业模拟舱：`simulator_scenario`、`simulator_node` `[v2.0新增]`、`simulator_node_option` `[v2.0新增]`、`simulator_session`、`simulator_choice`
- 成就徽章：`badge_template`、`user_badge`、`badge_lock_log`

### 5.2 设计规范 `[v2.0新增]`

1. **移除所有跨模块物理外键**，仅保留索引，关联性由应用层校验（支撑未来分库与数据迁移）。
2. **所有业务表统一含**：`is_deleted tinyint default 0`、`created_at`、`updated_at`（MyBatis-Plus 自动填充）。
3. **枚举值统一抽 `iwantjob-common/enums` 包**，DB 注释标注枚举类名。
4. **并发控制**：余额类字段（如 `mutual_points.balance`）配 `version` 乐观锁。
5. **软删除统一**：MyBatis-Plus `@TableLogic`，删除即 `is_deleted=1`。

### 5.3 完整建表SQL（修订版）

```sql
CREATE DATABASE IF NOT EXISTS `iwantjob` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE `iwantjob`;

-- ==================== 用户与认证 ====================
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(255) NOT NULL,
  `email` varchar(100) DEFAULT NULL,
  `phone` varchar(20) DEFAULT NULL,
  `role` tinyint NOT NULL DEFAULT '0' COMMENT 'UserRoleEnum: 0-学生,1-校友,2-HR,3-导师,9-管理员',
  `real_name` varchar(50) DEFAULT NULL,
  `avatar_url` varchar(255) DEFAULT NULL,
  `status` tinyint DEFAULT '1' COMMENT '0-禁用,1-正常',
  `last_login` datetime DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  UNIQUE KEY `uk_email` (`email`),
  UNIQUE KEY `uk_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `user_profile` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `school` varchar(100) DEFAULT NULL,
  `major` varchar(100) DEFAULT NULL,
  `graduation_year` int DEFAULT NULL,
  `skills` varchar(500) DEFAULT NULL,
  `bio` varchar(500) DEFAULT NULL,
  `available_status` tinyint DEFAULT '1',
  `resume_id` bigint DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `user_auth` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `identity_type` varchar(30) NOT NULL,
  `id_card_image` varchar(255) DEFAULT NULL,
  `verified` tinyint DEFAULT '0',
  `verify_comment` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_user_auth` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- [v2.0新增] 审计日志
CREATE TABLE `sys_audit_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint DEFAULT NULL,
  `action` varchar(50) NOT NULL COMMENT '操作类型，如 SALARY_REVIEW/BADGE_LOCK/POINT_ADJUST/USER_BAN',
  `target_type` varchar(30) DEFAULT NULL,
  `target_id` bigint DEFAULT NULL,
  `detail` json DEFAULT NULL,
  `ip` varchar(50) DEFAULT NULL,
  `ua` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_audit_user` (`user_id`),
  KEY `idx_audit_action` (`action`),
  KEY `idx_audit_time` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 职位相关 ====================
CREATE TABLE `job` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL,
  `company_name` varchar(100) NOT NULL,
  `job_type` tinyint NOT NULL COMMENT 'JobTypeEnum: 0-实习,1-校招,2-社招',
  `description` text,
  `requirements` text,
  `salary_range` varchar(50) DEFAULT NULL,
  `location` varchar(100) DEFAULT NULL,
  `source` tinyint DEFAULT '0',
  `contact_email` varchar(100) DEFAULT NULL,
  `expiry_date` datetime DEFAULT NULL,
  `view_count` int DEFAULT '0',
  `poster_id` bigint DEFAULT NULL,
  `status` tinyint DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_job_title` (`title`),
  KEY `idx_job_company` (`company_name`),
  KEY `idx_job_location` (`location`),
  KEY `idx_job_type` (`job_type`),
  KEY `idx_job_expiry` (`expiry_date`),
  KEY `idx_job_poster` (`poster_id`),
  FULLTEXT KEY `ft_job_search` (`title`,`description`,`requirements`)  -- [v2.0] ES未上时兜底
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `job_application` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `job_id` bigint NOT NULL,
  `user_id` bigint NOT NULL,
  `resume_id` bigint DEFAULT NULL,
  `cover_letter` text,
  `status` tinyint DEFAULT '0' COMMENT 'ApplicationStatusEnum: 0-投递成功,1-初筛,2-面试,3-录用,4-拒绝',
  `hr_remark` varchar(255) DEFAULT NULL,
  `applied_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_application_job` (`job_id`),
  KEY `idx_application_user` (`user_id`),
  KEY `idx_application_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `internal_referral` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `job_id` bigint NOT NULL,
  `referral_code` varchar(20) NOT NULL,
  `max_count` int DEFAULT '10',
  `used_count` int DEFAULT '0',
  `status` tinyint DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_referral_code` (`referral_code`),
  KEY `idx_referral_user` (`user_id`),
  KEY `idx_referral_job` (`job_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 简历 ====================
CREATE TABLE `resume` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `title` varchar(100) DEFAULT NULL,
  `content_json` json NOT NULL,
  `ai_score` int DEFAULT NULL,
  `is_default` tinyint DEFAULT '0',
  `version` int DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_resume_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `resume_optimization_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `resume_id` bigint NOT NULL,
  `user_id` bigint NOT NULL,
  `original_text` text,
  `optimized_text` text,
  `type` tinyint NOT NULL COMMENT 'OptimTypeEnum: 0-润色,1-翻译,2-强化',
  `feedback` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_optim_log_resume` (`resume_id`),
  KEY `idx_optim_log_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 模拟面试 ====================
CREATE TABLE `mock_interview` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `type` tinyint NOT NULL COMMENT 'InterviewTypeEnum: 0-技术,1-行为,2-综合',
  `difficulty` tinyint DEFAULT '1',
  `target_job` varchar(100) DEFAULT NULL,
  `status` tinyint DEFAULT '0' COMMENT 'MockStatusEnum: 0-进行中,1-完成,2-中断',
  `score_summary` json DEFAULT NULL,
  `start_time` datetime DEFAULT NULL,
  `end_time` datetime DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_mock_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `interview_question` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `mock_id` bigint NOT NULL,
  `question_text` text NOT NULL,
  `answer_text` text,
  `ai_feedback` text,
  `reference_answer` text,
  `sort_order` int DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_iq_mock` (`mock_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `question_bank` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `category` tinyint NOT NULL COMMENT 'InterviewTypeEnum: 0-技术,1-行为,2-综合',
  `sub_category` varchar(50) DEFAULT NULL,
  `question_text` text NOT NULL,
  `expected_keywords` text,
  `difficulty` tinyint DEFAULT NULL,
  `created_by` bigint DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_qb_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 社区 ====================
CREATE TABLE `post` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `author_id` bigint NOT NULL,
  `type` tinyint NOT NULL COMMENT 'PostTypeEnum: 0-问答,1-面经,2-技能交换,3-生活互助,4-其他',
  `title` varchar(200) NOT NULL,
  `content` text,
  `tags` varchar(200) DEFAULT NULL,
  `view_count` int DEFAULT '0',
  `like_count` int DEFAULT '0',
  `is_pinned` tinyint DEFAULT '0',
  `is_solved` tinyint DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_post_author` (`author_id`),
  KEY `idx_post_type` (`type`),
  KEY `idx_post_created` (`created_at`),
  FULLTEXT KEY `ft_post_search` (`title`,`content`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `answer` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `post_id` bigint NOT NULL,
  `author_id` bigint NOT NULL,
  `content` text NOT NULL,
  `is_accepted` tinyint DEFAULT '0',
  `like_count` int DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_answer_post` (`post_id`),
  KEY `idx_answer_author` (`author_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `skill_exchange` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `from_user_id` bigint NOT NULL,
  `to_user_id` bigint DEFAULT NULL,
  `offer_skill` varchar(50) DEFAULT NULL,
  `want_skill` varchar(50) DEFAULT NULL,
  `status` tinyint DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_skill_from` (`from_user_id`),
  KEY `idx_skill_to` (`to_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `crowdfunding_project` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `initiator_id` bigint NOT NULL,
  `title` varchar(100) DEFAULT NULL,
  `description` text,
  `goal_amount` decimal(10,2) NOT NULL,
  `current_amount` decimal(10,2) DEFAULT '0.00',
  `status` tinyint DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `end_date` datetime DEFAULT NULL,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_cf_initiator` (`initiator_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 帮帮团 ====================
CREATE TABLE `help_group_request` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `applicant_id` bigint NOT NULL,
  `reason_type` tinyint NOT NULL COMMENT 'ReasonTypeEnum: 0-投递失败,1-面试屡败,2-职业迷茫,3-其他',
  `description` text,
  `match_tags` varchar(200) DEFAULT NULL COMMENT '[v2.0新增] 匹配标签，如 目标行业/城市，支撑自动匹配',
  `status` tinyint DEFAULT '0' COMMENT 'HelpStatusEnum: 0-待匹配,1-已匹配,2-完成,3-关闭',
  `supporter_id` bigint DEFAULT NULL,
  `matched_at` datetime DEFAULT NULL,
  `resolved_at` datetime DEFAULT NULL,
  `feedback` text,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_help_applicant` (`applicant_id`),
  KEY `idx_help_status` (`status`),
  KEY `idx_help_supporter` (`supporter_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 积分（归属 user 模块） ====================
CREATE TABLE `mutual_points` (
  `user_id` bigint NOT NULL,
  `balance` int DEFAULT '0',
  `total_earned` int DEFAULT '0',
  `version` int DEFAULT '0' COMMENT '[v2.0新增] 乐观锁版本号',
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `point_transaction` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `points` int NOT NULL COMMENT '正数增加，负数扣减',
  `reason` varchar(100) NOT NULL,
  `related_id` bigint DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_pt_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `unlock_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `benefit` tinyint NOT NULL COMMENT 'BenefitEnum: 0-导师咨询,1-高级简历优化,2-模拟面试次数,3-其他',
  `cost_points` int NOT NULL,
  `status` tinyint DEFAULT '1',
  `used_at` datetime DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_unlock_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== AI 对话 ====================
CREATE TABLE `ai_conversation` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `scene` tinyint NOT NULL COMMENT 'AiSceneEnum: 0-简历,1-面试,2-职位推荐,3-通用',
  `title` varchar(100) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_ai_conv_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `ai_message` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `conversation_id` bigint NOT NULL,
  `role` varchar(10) NOT NULL COMMENT 'user/assistant',
  `content` text NOT NULL,
  `token_count` int DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_ai_msg_conv` (`conversation_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 薪资白皮书（创新1） ====================
CREATE TABLE `salary_report_data` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL COMMENT '仅用于积分发放与去重，对外查询禁止按user_id反查明细',
  `city` varchar(50) DEFAULT NULL,
  `position` varchar(100) DEFAULT NULL,
  `salary_min` int DEFAULT NULL,
  `salary_max` int DEFAULT NULL,
  `company_scale` varchar(30) DEFAULT NULL COMMENT '仅规模档位，不存公司全称',
  `industry` varchar(50) DEFAULT NULL,
  `job_type` tinyint DEFAULT NULL COMMENT 'JobTypeEnum: 0-实习,1-全职',
  `education_level` tinyint DEFAULT NULL COMMENT 'EduEnum: 0-专科,1-本科,2-硕士,3-博士,4-其他',
  `is_double_first_class` tinyint DEFAULT NULL COMMENT '0-否,1-是',
  `offer_month` char(7) DEFAULT NULL,
  `is_anonymous` tinyint DEFAULT '1' COMMENT '默认1且不可改为0',
  `verified` tinyint DEFAULT '0' COMMENT '0-待审核,1-通过,2-驳回',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_salary_user` (`user_id`),
  KEY `idx_salary_city_pos` (`city`, `position`),
  KEY `idx_salary_verified` (`verified`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `salary_whitepaper` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `version` varchar(20) DEFAULT NULL,
  `title` varchar(100) DEFAULT NULL,
  `report_json` json,
  `generated_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `access_level` tinyint DEFAULT '0' COMMENT '0-公开,1-贡献者专属',
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `salary_contribution_reward` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `report_data_id` bigint DEFAULT NULL,
  `points_awarded` int DEFAULT '0',
  `unlock_match_boost` tinyint DEFAULT '0',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_scr_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- [v2.0新增] 薪资审核流转日志
CREATE TABLE `salary_review_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `report_data_id` bigint NOT NULL,
  `reviewer_id` bigint DEFAULT NULL,
  `action` varchar(20) NOT NULL COMMENT 'APPROVE/REJECT',
  `comment` varchar(255) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_review_data` (`report_data_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== AI 职业模拟舱（创新2） ====================
CREATE TABLE `simulator_scenario` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `title` varchar(100) NOT NULL,
  `type` tinyint NOT NULL COMMENT 'ScenarioTypeEnum: 0-入职,1-向上汇报,2-冲突处理,3-跨部门协作',
  `description` text,
  `initial_context` text,
  `start_node_id` bigint DEFAULT NULL COMMENT '[v2.0新增] 起始节点',
  `difficulty` tinyint DEFAULT '1',
  `is_active` tinyint DEFAULT '1',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- [v2.0新增] 节点表（显式建模分支图）
CREATE TABLE `simulator_node` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `scenario_id` bigint NOT NULL,
  `node_desc` text NOT NULL,
  `ai_prompt_snippet` text DEFAULT NULL COMMENT '注入大模型的场景片段提示',
  `is_end` tinyint DEFAULT '0',
  `sort_order` int DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_node_scenario` (`scenario_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- [v2.0新增] 节点选项表
CREATE TABLE `simulator_node_option` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `node_id` bigint NOT NULL,
  `option_text` varchar(500) NOT NULL,
  `next_node_id` bigint DEFAULT NULL,
  `soft_skill_tags` varchar(200) DEFAULT NULL COMMENT '选择该项触发的软技能标签',
  PRIMARY KEY (`id`),
  KEY `idx_option_node` (`node_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `simulator_session` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `scenario_id` bigint NOT NULL,
  `status` tinyint DEFAULT '0' COMMENT 'SimStatusEnum: 0-进行中,1-已完成,2-中断',
  `current_node_id` bigint DEFAULT NULL,
  `started_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `completed_at` datetime DEFAULT NULL,
  `overall_score` int DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `idx_sim_user` (`user_id`),
  KEY `idx_sim_scenario` (`scenario_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `simulator_choice` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `session_id` bigint NOT NULL,
  `node_id` bigint DEFAULT NULL,
  `node_desc` text,
  `options_json` json COMMENT '[v2.0变更] 当时呈现的选项快照',
  `option_id` bigint DEFAULT NULL COMMENT '[v2.0变更] 用户选择的选项ID',
  `user_choice` varchar(500) DEFAULT NULL COMMENT '选项文本冗余',
  `ai_feedback` text,
  `soft_skill_tags` varchar(200) DEFAULT NULL,
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_choice_session` (`session_id`),
  KEY `idx_choice_node` (`node_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- ==================== 成就徽章（创新3） ====================
CREATE TABLE `badge_template` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL,
  `description` varchar(200) DEFAULT NULL,
  `icon_url` varchar(255) DEFAULT NULL,
  `condition_type` tinyint NOT NULL COMMENT 'BadgeCondEnum: 0-分享面经次数,1-帮助他人次数,2-薪资贡献,3-模拟舱完成,4-项目合作评价',
  `threshold` int DEFAULT '1',
  `rarity` tinyint DEFAULT '0' COMMENT 'RarityEnum: 0-普通,1-稀有,2-史诗',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` tinyint DEFAULT '0',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `user_badge` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `badge_id` bigint NOT NULL,
  `earned_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `is_locked` tinyint DEFAULT '0' COMMENT '铸造后立即置1，不可回退',
  `lock_hash` varchar(64) DEFAULT NULL COMMENT 'SHA256(user_id+badge_id+earned_at+system_salt)',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_badge` (`user_id`, `badge_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- [v2.0新增] 防篡改约束：仅允许INSERT，禁止UPDATE/DELETE（DB触发器）
DELIMITER //
CREATE TRIGGER `trg_user_badge_no_update`
BEFORE UPDATE ON `user_badge`
FOR EACH ROW
BEGIN
    SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'user_badge is immutable after insert';
END//
CREATE TRIGGER `trg_user_badge_no_delete`
BEFORE DELETE ON `user_badge`
FOR EACH ROW
BEGIN
    SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'user_badge is immutable after insert';
END//
DELIMITER ;

CREATE TABLE `badge_lock_log` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_badge_id` bigint NOT NULL,
  `operated_by` bigint DEFAULT NULL COMMENT 'null表示系统自动',
  `action` varchar(20) DEFAULT 'LOCK',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_lock_badge` (`user_badge_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 6. 核心API设计 `[v2.0变更：补充缺失接口+标注权限]`

> 所有接口统一返回 `Result<T>`，写接口需 `X-Idempotency-Key`。权限标记：[S]学生 [A]校友 [H]HR [T]导师 [Admin]管理员

### 6.1 基础模块API

| 模块 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|:----:|------|
| 认证 | POST | /auth/register | 公开 | 用户注册 |
| | POST | /auth/login | 公开 | 登录返回JWT |
| | POST | /auth/refresh | [已登录] | `[v2.0新增]`刷新token |
| | POST | /auth/logout | [已登录] | `[v2.0新增]`登出，JWT入Redis黑名单 |
| 文件 | POST | /files/upload | [已登录] | `[v2.0新增]`通用文件上传 |
| 用户 | GET | /user/me | [已登录] | 当前用户信息 |
| | PUT | /user/profile | [已登录] | 更新个人资料 |
| | GET | /user/badges | [已登录] | 我的徽章 |
| | GET | /badges/user/{userId} | 公开 | `[v2.0新增]`公开主页徽章展示 |
| 职位 | GET | /jobs?keyword=&type= | [已登录] | 职位搜索(ES/FULLTEXT) |
| | GET | /jobs/{id} | [已登录] | 职位详情 |
| | POST | /jobs | [A][H][Admin] | 发布职位 |
| | POST | /jobs/{id}/apply | [S][A] | 投递职位 |
| 内推 | POST | /referrals | [A] | `[v2.0补充]`创建内推码 |
| 简历 | POST | /resume/optimize | [S][A] | AI简历优化 |
| | POST | /resume/score | [S][A] | AI简历评分 |
| | GET | /resume/match?jobId= | [S][A] | 简历-职位匹配度 |
| 模拟面试 | POST | /interview/start | [S][A] | 创建面试会话 |
| | POST | /interview/answer | [S][A] | 提交回答获得反馈 |
| | GET | /interview/history | [已登录] | 面试历史 |
| 社区 | POST | /posts | [已登录] | 发帖 |
| | GET | /posts/{id} | [已登录] | 帖子详情 |
| | POST | /posts/{id}/answer | [已登录] | 回答问题 |
| | PUT | /answers/{id}/accept | [帖子作者] | 采纳答案 |
| 互助任务 | POST | /tasks/share-experience | [已登录] | 记录分享面经(得积分) |
| | GET | /points/me | [已登录] | 我的积分 |
| | POST | /unlock/mentor | [S][A] | 积分解锁导师咨询 |
| 帮帮团 | POST | /help-group/request | [S][A] | 发起求助 |
| | POST | /help-group/{id}/match | [A][T][Admin] | 匹配支援者 |
| | PUT | /help-group/{id}/resolve | [参与方] | 完成支援 |
| AI对话 | POST | /ai/chat | [已登录] | 通用AI对话 |

### 6.2 创新模块API

| 模块 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|:----:|------|
| 薪资白皮书 | POST | /salary/contribute | [S][A] | 提交脱敏薪资数据 |
| | GET | /salary/whitepaper/latest | [已登录] | 获取最新白皮书(简版) |
| | GET | /salary/whitepaper/{id} | [已登录] | `[v2.0新增]`指定版本白皮书 |
| | GET | /salary/match/boost | [贡献者] | 贡献者专属精准职位推送 |
| 职业模拟舱 | GET | /simulator/scenarios | [S][A][T] | 获取可用场景列表 |
| | POST | /simulator/start?scenarioId= | [S][A] | 开始模拟会话 |
| | POST | /simulator/choose | [S][A] | 提交选择获取下一节点与反馈 |
| | GET | /simulator/session/{id}/report | [会话所有者] | 获取会话完整报告与评分 |
| 成就徽章 | GET | /badges/templates | [已登录] | 徽章模板列表 |
| | GET | /badges/verify?userId=&badgeId=&hash= | 公开 | `[v2.0新增]`企业查验接口(预留) |

### 6.3 管理后台API `[v2.0新增]`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /admin/salary/pending | 待审核薪资数据列表 |
| PUT | /admin/salary/{id}/review | 审核薪资数据(APPROVE/REJECT) |
| GET | /admin/users | 用户管理列表 |
| PUT | /admin/users/{id}/status | 封禁/解封用户 |
| GET | /admin/badges/templates | 徽章模板管理 |
| POST | /admin/badges/templates | 新建徽章模板 |
| POST | /admin/badges/lock/{userBadgeId} | 系统锁定徽章(预留手动锁定) |
| POST | /admin/whitepaper/generate | 手动触发白皮书生成 |

## 7. 关键业务设计

### 7.1 薪资白皮书与精准匹配闭环

**贡献流程（脱敏）**：用户填写脱敏表单（城市、岗位、薪资范围、公司规模档位、行业、实习/全职、学历层次、是否双一流），不收集姓名/身份证/公司全称。提交后进入审核队列。`user_id` 仅用于积分发放与去重，对外查询接口禁止按 user_id 反查薪资明细。`is_anonymous` 默认 1 且不可改。

**审核规则（自动 + 人工）** `[v2.0明确]`：
1. 薪资范围合理性：≥ 城市/学历对应最低工资，≤ 行业 P99（用历史数据计算）。
2. 同用户同月同岗位去重（Redis Set 判重）。
3. 超出历史 3σ 的标记为待人工复核。
4. 审核动作写 `salary_review_log`（APPROVE/REJECT + 意见）。

**权益回馈**：每次有效贡献 +30 积分（乐观锁更新 `mutual_points`，写 `point_transaction`）。连续 ≥3 次有效贡献或高可信数据，置 `unlock_match_boost=1`，解锁精准匹配优先权。

**白皮书生成**：定时任务（每月 1 日）聚合已审核薪资数据，按城市/岗位/学历/行业分组计算 P25/P50/P75/P99、样本量、同比。调用千问生成分析文字 + 图表数据，写入 `report_json`。计算与 AI 生成异步执行（RabbitMQ 或 `@Async`），避免长事务。

**访问控制**：简版白皮书公开；高级章节（按学历/双一流对比）需积分解锁或贡献记录 ≥1，写 `unlock_record`。

**ES 双写一致性** `[v2.0补充]`：职位变更先落 MySQL，再发 MQ，ES 消费者更新索引；失败重试 + 定时对账。

### 7.2 AI职业模拟舱：文本分支职场演练

**场景建模** `[v2.0变更：显式节点图]`：采用 `simulator_scenario` → `simulator_node` → `simulator_node_option` 三级结构显式建模分支图，便于场景可视化编辑与复盘。每个节点含 `node_desc`（情境描述）和 `ai_prompt_snippet`（注入大模型的场景片段）。

**分支机制**：用户进入场景 → 读起始节点（Redis 缓存，miss 查 MySQL）→ AI 基于 node_desc + prompt_snippet 生成情境描述与 2~3 选项（选项也可由 `simulator_node_option` 预定义）。用户选择 → 写 `simulator_choice` → 调千问生成即时反馈 → 按 `next_node_id` 推进 → 直至 `is_end` 节点。

**一致性** `[v2.0明确]`：每个 choice 立即落 MySQL，Redis 仅缓存当前节点；会话恢复优先读 Redis，miss 则从 MySQL 重建。

**评分体系**：到达结束节点后，调千问基于全程 choices 生成软技能评分（沟通协作/应变能力/抗压/跨部门协作），存入 `overall_score` + 详细反馈，并触发徽章事件。

**Prompt 规范** `[v2.0补充]`：系统提示词固定角色设定 + "不得跳出职场场景"约束 + 输出格式（情境描述 + 选项 JSON），保证分支一致性。

### 7.3 防篡改可信徽章 `[v2.0变更]`

**措辞定调**：对外统一表述为"**防篡改可信徽章**"——基于数据库约束 + 哈希指纹 + 审计日志实现业务层防伪与可追溯，不宣称密码学层面不可篡改。

**自动铸造（事件驱动）** `[v2.0变更]`：
- 定义 `BadgeTriggerEvent(userId, conditionType, refId)`。
- 触发源：community（分享面经 N 次）、helpgroup（帮扶完成）、simulator（完成场景）、salary（有效贡献 N 次）在关键行为后发布事件。
- badge 模块消费事件 → 查 `badge_template` 匹配条件 → 达阈值且未持有 → 铸造，避免定时扫描延迟。

**锁定与哈希**：铸造时 `is_locked=1`，`lock_hash = SHA256(user_id + badge_id + earned_at + system_salt)`。`user_badge` 表通过 DB 触发器禁止 UPDATE/DELETE（仅允许 INSERT）。写 `badge_lock_log`（action=LOCK, operated_by=null 表示系统）。

**展示与扩展**：个人主页展示已锁定徽章（含 lock_hash 前 8 位作为指纹）。预留企业查验接口 `GET /badges/verify` 供未来扩展。

### 7.4 互助解锁就业权益

**任务类型**：分享面经、回答他人问题、推荐职位、参与模拟面试点评。
**积分规则**：每个有效互助行为获得固定积分，管理员可审核。
**并发安全** `[v2.0补充]`：积分扣减采用"预扣—确认"两阶段 + 乐观锁，防止超兑。Redis 记录每日任务完成次数，MQ 异步增加积分，MySQL 记录解锁记录。
**可兑换权益**：导师 1 对 1 咨询、AI 高级简历优化次数、模拟面试时长增加。

## 8. 统一工程规范 `[v2.0新增章节]`

### 8.1 统一响应体

```java
@Data
public class Result<T> {
    private int code;       // 0=成功，非0=错误码
    private String message;
    private T data;
    private long timestamp;
    public static <T> Result<T> success(T data) { ... }
    public static Result<Void> fail(ErrorCode ec) { ... }
}
```

### 8.2 统一错误码

| 段位 | 含义 |
|------|------|
| 0 | 成功 |
| 1xxxx | 通用错误（参数/鉴权/限流/系统） |
| 2xxxx | 用户模块 |
| 3xxxx | 职位模块 |
| 4xxxx | 简历模块 |
| 5xxxx | 面试模块 |
| 6xxxx | 社区/帮帮团 |
| 7xxxx | 薪资白皮书 |
| 8xxxx | 模拟舱 |
| 9xxxx | 徽章 |

### 8.3 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)                  // 业务异常
    @ExceptionHandler(MethodArgumentNotValidException.class)    // 参数校验
    @ExceptionHandler(AccessDeniedException.class)              // 权限
    @ExceptionHandler(Exception.class)                          // 兜底
}
```

### 8.4 日志规范

- 统一 JSON 格式（logback + logstash-encoder），含 `traceId`（MDC + 拦截器注入）。
- 分级：DEBUG（开发）/ INFO（关键业务动作）/ WARN（可恢复异常）/ ERROR（需告警）。
- 敏感字段脱敏（手机号、身份证、薪资明细分词）。

### 8.5 接口幂等

- 写接口要求前端传 `X-Idempotency-Key`（UUID），后端 Redis SETNX 校验，TTL 10 分钟。
- 适用：投递职位、提交薪资贡献、发起求助、积分扣减。

### 8.6 限流

- 接口级：基于 Redis + Lua 的令牌桶，按用户 + 接口维度。
- AI 类接口（简历优化、模拟舱、对话）单独限流：每用户每分钟 N 次，保护千问配额。

### 8.7 数据权限（`@DataScope` 注解 + MyBatis 拦截器拼 SQL 条件）

| 角色 | 数据范围 |
|------|----------|
| 学生 | 本人所有数据 + 公开职位/帖子/白皮书简版 |
| 校友 | 同学生 + 可发起内推 |
| HR | 本公司发布的职位及对应投递 |
| 导师 | 本人帮扶的帮帮团请求 + 可点评的模拟面试 |
| 管理员 | 薪资审核、用户管理、徽章模板、白皮书发布 |

### 8.8 审计日志（`@AuditLog` 切面）

独立 `sys_audit_log` 表。关键操作必须标注：薪资审核、徽章锁定/解锁、积分调整、用户封禁。

### 8.9 多环境配置

`application-{dev,test,prod}.yml`，敏感配置走环境变量；本地用 `docker-compose` 一键起中间件。

### 8.10 单元测试

- Service 层 JUnit5 + Mockito，覆盖率目标核心模块 ≥ 60%。
- API 层 MockMvc 冒烟测试。

## 9. 角色权限矩阵 `[v2.0新增]`

| 接口 | 学生 | 校友 | HR | 导师 | 管理员 |
|------|:----:|:----:|:--:|:----:|:------:|
| /auth/* | ✅ | ✅ | ✅ | ✅ | ✅ |
| /jobs (发布) | ❌ | ✅(内推) | ✅ | ❌ | ✅ |
| /jobs/apply | ✅ | ✅ | ❌ | ❌ | ❌ |
| /salary/contribute | ✅ | ✅ | ❌ | ❌ | ❌ |
| /salary/whitepaper/latest | 简版 | 简版 | 简版 | 简版 | 全量 |
| /admin/salary/review | ❌ | ❌ | ❌ | ❌ | ✅ |
| /simulator/* | ✅ | ✅ | ❌ | ✅(点评) | ✅ |
| /admin/badges/lock | ❌ | ❌ | ❌ | ❌ | ✅ |
| /help-group/request | ✅ | ✅ | ❌ | ❌ | ❌ |
| /help-group/match | ❌ | ✅ | ❌ | ✅ | ✅ |

## 10. 开发阶段里程碑（MVP/完整版双轨，假设1人） `[v2.0变更]`

| 阶段 | 周期 | 内容（MVP 优先） |
|------|------|------------------|
| 一 | 1w | 骨架 + 用户认证 + MySQL/Redis + 统一规范（响应/异常/日志/审计） |
| 二 | 1w | 职位 CRUD + 投递（MySQL FULLTEXT 兜底）+ 文件上传 + 内推 |
| 三 | 1.5w | AI 简历优化/评分/匹配 + 模拟面试最小闭环 |
| 四 | 1w | 社区问答 + 积分（乐观锁）+ **薪资白皮书最小闭环**（贡献+审核+简版报告） |
| 五 | 1w | **AI 模拟舱**（入职 + 冲突两场景）+ 帮帮团 |
| 六 | 1w | **防篡改徽章** + 事件驱动打通 |
| 七 | 1w | 完整版扩展：ES 搜索、RabbitMQ 异步、技能交换、众筹、白皮书高级章节 |
| 八 | 1w | 集成测试、Knife4j 文档、压测、毕设论文素材整理 |
| 九 | 0.5w | 缓冲 + 答辩准备 |

> 创新点全部落在 MVP 内（第四~六周），保证答辩有完整演示；ES/MQ 等重型组件放第七周作为增强。

## 11. 风险与对策 `[v2.0新增]`

| 风险 | 等级 | 对策 |
|------|------|------|
| 千问 API 配额/费用 | 高 | 严格限流 + 本地 Mock 开发 + 缓存相似 prompt 结果 |
| 中间件部署运维 | 中 | 分级引入，MVP 只用 MySQL+Redis |
| 9 周工期紧张 | 高 | 双轨制，保 MVP；创新点优先演示 |
| "不可篡改"被质疑 | 中 | 措辞改为"防篡改可信"+ 哈希指纹 + 审计日志 |
| 薪资数据造假 | 中 | 自动审核规则 + 异常值人工复核 + 去重 |
| ES 双写不一致 | 低 | MQ 异步 + 定时对账 |

## 12. 创新点总结

1. **薪资透明计划**：打破大学生就业信息差，用社区众筹数据生成动态白皮书，贡献者获得职位匹配特权。
2. **AI职业模拟舱**：将软技能训练产品化，以互动剧情填补市场空白，让求职准备不止于简历和面试。
3. **防篡改可信徽章体系**：通过数据库约束 + 哈希指纹 + 审计日志实现可信成就记录，为大学生构建可验证的"软实力档案"。
4. **互助经济闭环**：保留原方案积分兑换、帮帮团机制，将社区贡献与就业权益深度绑定。

---

**本文档为"我要工作"平台优化版最终稿（v2.0），已落实审批报告全部必要修订与可选优化，可直接用于指导编码与毕设实施。**

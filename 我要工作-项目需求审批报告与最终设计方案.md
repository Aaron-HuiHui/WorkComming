# 《我要工作》平台 — 项目需求审批报告与最终设计方案

> 审批对象：《我要工作 — 大学生智能就业与互助成长平台 开发文档（最终版）》
> 审批维度：技术栈 / 系统架构 / 数据库 / API / 业务逻辑 / 工程规范 / 里程碑可行性
> 审批结论：**有条件通过**（需按本报告"修订要点"调整后，方可作为开发与毕设实施的最终基准）
> 文档性质：在原文档基础上的**增量审批 + 修订补充**，未重复原文已合理的内容

---

## 一、审批摘要

| 维度 | 评级 | 一句话结论 |
|------|------|------------|
| 业务定位与创新点 | ✅ 优秀 | 三大创新点差异化清晰，闭环逻辑成立 |
| 技术栈选型 | ⚠️ 偏重 | 主流合理，但中间件过多，毕设场景需精简或分阶段 |
| 系统架构 | ⚠️ 待优化 | 多模块划分清晰，但 AI 模块耦合、领域边界未显式化 |
| 数据库设计 | ⚠️ 需修订 | 表覆盖完整，但物理外键过重、缺并发控制与若干规范字段 |
| API 设计 | ⚠️ 需补充 | 主流程覆盖，缺管理后台、文件上传、刷新令牌、统一规范 |
| 业务逻辑 | ⚠️ 需严谨化 | 创新点措辞需修正，触发机制/并发安全/一致性未明确 |
| 工程规范 | ❌ 缺失 | 统一响应、异常、日志、幂等、限流、权限矩阵均未定义 |
| 里程碑可行性 | ⚠️ 偏紧 | 9 周体量偏大，需明确人力或设定 MVP/完整版双轨 |

**总体判断**：需求文档完成度高、创新性强，**具备立项条件**；但存在两类必须处理的问题——
1. **"措辞-实现"落差**：如"不可篡改"在数据库层面并不成立，答辩与评审会被质疑；
2. **工程规范空白**：缺少统一约定，开发中后期必然返工。

本报告第二部分给出修订要点，第三部分给出最终版设计方案的关键补充章节。

---

## 二、问题清单与修订要点

### 2.1 技术栈（建议：分级引入）

**问题**：本地需同时部署 MySQL / Redis / RabbitMQ / MinIO / Elasticsearch 五套中间件，开发与运维成本高；ES 在数据量不大时收益有限。

**修订建议**：划分为"核心必选"与"增强可选"两档，按里程碑渐进引入。

| 分级 | 组件 | 引入阶段 |
|------|------|----------|
| 必选 | MySQL、Redis | 第一阶段起 |
| 必选 | MinIO（简历/白皮书文件） | 简历模块起 |
| 可选 | RabbitMQ | 薪资白皮书异步入库时引入；之前可用 Spring `@Async` 替代 |
| 可选 | Elasticsearch | 职位数据达一定量或第二阶段再上；之前用 MySQL `FULLTEXT` + Like 兜底 |

> 这样第一阶段只需 MySQL + Redis，显著降低启动门槛，且不影响创新点落地。

### 2.2 系统架构

**问题 A：AI 模块耦合**
`iwantjob-ai` 集中所有 AI 能力，而 `simulator`、`interview`、`resume` 都要调用它，形成跨模块反向依赖。

**修订建议**：将 AI 能力拆为两层——
- `iwantjob-ai`：仅封装千问 SDK 调用、鉴权、重试、限流（**基础设施层**，被各业务模块依赖）；
- 各业务模块（simulator/interview/resume）**自带业务 prompt 编排逻辑**，仅调用 ai 的 `chat(prompt, history)` 原子能力。
- 模块间依赖方向：业务模块 → common/framework → ai-sdk，禁止 ai 反向依赖业务。

**问题 B：领域边界未显式化**
10 个业务模块之间存在共享概念（如"用户积分"被 community、helpgroup、salary 共同写入）。

**修订建议**：明确"积分"归属 `user` 模块（或新建 `iwantjob-points`），其他模块通过事件/MQ 异步通知积分变更，**禁止跨模块直接写彼此表**。

### 2.3 数据库（需修订项）

| # | 问题 | 修订建议 |
|---|------|----------|
| D1 | 大量物理外键，违反模块边界，阻碍未来分库与数据迁移 | **移除所有跨模块外键**，改为应用层校验；仅保留模块内强关联（如 session→choice）外键可选 |
| D2 | `mutual_points.balance` 并发更新无保护 | 增加 `version int` 字段，采用乐观锁；或 Redis 预扣 + 异步落库 |
| D3 | `simulator_choice.options` 为 json 但 `next_node_id` 为单值，无法表达多分支 | 改为 `options` 内嵌 `[{option_id, text, next_node_id, ...}]`，去掉顶层 `next_node_id`；或新建 `simulator_node` 表显式建模节点图 |
| D4 | `salary_report_data` 仅 `is_anonymous` 字段不足以脱敏 | 贡献时即**不存储可逆关联**：`user_id` 仅用于积分发放后即清除可见性，对外查询禁止按 user_id 反查薪资明细；`verified` 需配套审核规则表 |
| D5 | `badge` 系列称"不可篡改"，但 DBA 可直接改库 | 措辞改为**"业务层防篡改 + 审计可追溯"**；如需更强保证，增设计算：`lock_hash = SHA256(user_id + badge_id + earned_at + system_salt)`，并在 `user_badge` 上加触发器禁止 update/delete（仅允许 insert） |
| D6 | 枚举值散落字段注释 | 统一抽取为 Java 枚举类（`common/enums`），并在数据库注释中标注枚举类名 |
| D7 | 部分表缺 `is_deleted` / `updated_at`（如 `post`、`badge_template`） | 统一规范：所有业务表含 `is_deleted tinyint default 0`、`created_at`、`updated_at`（MyBatis-Plus 自动填充） |
| D8 | `salary_whitepaper`、`badge_template` 等无软删除 | 同 D7 |
| D9 | 缺审核状态流转表 | 薪资数据需新增 `salary_review_log`（审核人、动作、意见、时间），支撑审计 |
| D10 | `help_group_request` 缺匹配规则字段 | 增加 `match_tags`（如目标行业/城市），支撑自动匹配 |

### 2.4 API（需补充）

**缺失接口**：
- `POST /auth/refresh`：刷新 token
- `POST /auth/logout`：登出（JWT 入 Redis 黑名单）
- `POST /files/upload`：通用文件上传（简历附件、头像、身份证）
- 管理后台：
  - `GET /admin/salary/pending`、`PUT /admin/salary/{id}/review`：薪资数据审核
  - `GET /admin/users`、`PUT /admin/users/{id}/status`：用户管理
  - `GET /admin/badges/templates`、`POST /admin/badges/templates`：徽章模板管理
- `GET /salary/whitepaper/{id}`：指定版本白皮书
- `GET /badges/user/{userId}`：公开主页徽章展示

**缺失规范**（详见第三部分）：
- 统一响应体 `Result<T>`、统一错误码、统一分页参数与返回结构
- 每个接口需标注所属角色权限

### 2.5 业务逻辑（需严谨化）

| # | 问题 | 修订建议 |
|---|------|----------|
| B1 | "不可篡改徽章"措辞 | 改为"防篡改可信徽章"，并在文档明确其安全边界（业务层防伪，非密码学不可篡改） |
| B2 | 徽章"自动铸造"触发时机未明确 | 采用**事件驱动**：定义 `BadgeTriggerEvent`，由 community/helpgroup/simulator 在关键行为后发布，badge 模块消费并判定条件；避免定时扫描的延迟 |
| B3 | 薪资审核规则模糊 | 定义明确规则：①薪资范围合理性（如月薪 ≥ 当地最低工资且 ≤ 行业 P99）；②同用户同月同岗位去重；③异常值（超出 3σ）人工复核 |
| B4 | 积分并发安全 | 见 D2；并定义积分扣减为"预扣—确认"两阶段，防止超兑 |
| B5 | ES 与 MySQL 双写一致性未提 | 采用**MQ 异步同步**：职位变更先落 MySQL，再发 MQ，ES 消费者更新索引；失败重试 + 定时对账 |
| B6 | 模拟舱会话状态在 Redis 的持久化策略未提 | 定义：每个 choice 立即落 MySQL，Redis 仅缓存当前节点；会话恢复时优先读 Redis，miss 则从 MySQL 重建 |
| B7 | JWT 黑名单清理 | 登出 token 写入 Redis，TTL = token 剩余有效期；过期自动清理 |

### 2.6 工程规范（必须补齐，详见第三部分）

统一响应体、全局异常处理、参数校验、日志规范、接口幂等、限流、数据权限、审计日志、多环境配置、单元测试策略——**原文档全部缺失，属硬伤**，在第三部分统一补齐。

### 2.7 里程碑可行性

**问题**：9 周完成 30 张表、10 模块、3 创新点，单人几无可能；团队规模未声明。

**修订建议**：采用 **MVP / 完整版双轨**，并声明人力假设（默认假设 1 人）。

- **MVP（必达，约 5 周）**：用户认证 + 职位（MySQL 搜索兜底）+ 简历 AI 优化 + 模拟面试 + 社区问答 + 积分 + **三大创新点各做一个最小闭环**。
- **完整版（冲刺，约 4 周）**：ES 搜索、RabbitMQ 异步、内推、帮帮团匹配、技能交换、众筹、白皮书高级章节、模拟舱多场景、徽章稀有度。
- 创新点优先级：薪资白皮书 > 模拟舱 > 徽章（前两者更易演示，答辩效果好）。

---

## 三、最终版设计方案（补充章节）

> 以下为在原文档之上**新增/修订**的关键设计，与原文档合并即为最终版。

### 3.1 统一工程规范

#### 3.1.1 统一响应体

```java
@Data
public class Result<T> {
    private int code;       // 0=成功，非0=错误码
    private String message;
    private T data;
    private long timestamp;

    public static <T> Result<T> success(T data) { ... }
    public static Result<Void> fail(ErrorCode ec) { ... }
}
```

#### 3.1.2 统一错误码

| 段位 | 含义 |
|------|------|
| 0 | 成功 |
| 1xxxx | 通用错误（参数/鉴权/限流/系统） |
| 2xxxx | 用户模块 |
| 3xxxx | 职位模块 |
| 4xxxx | 简历模块 |
| 5xxxx | 面试模块 |
| 6xxxx | 社区/帮帮团 |
| 7xxxx | 薪资白皮书 |
| 8xxxx | 模拟舱 |
| 9xxxx | 徽章 |

#### 3.1.3 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)   // 业务异常
    @ExceptionHandler(MethodArgumentNotValidException.class) // 参数校验
    @ExceptionHandler(AccessDeniedException.class) // 权限
    @ExceptionHandler(Exception.class)            // 兜底
}
```

#### 3.1.4 日志规范

- 统一 JSON 格式（logback + logstash-encoder），含 `traceId`（MDC + 拦截器注入）。
- 分级：DEBUG（开发）/ INFO（关键业务动作）/ WARN（可恢复异常）/ ERROR（需告警）。
- 敏感字段脱敏（手机号、身份证、薪资明细分词）。

#### 3.1.5 接口幂等

- 写接口要求前端传 `X-Idempotency-Key`（UUID），后端 Redis SETNX 校验，TTL 10 分钟。
- 适用：投递职位、提交薪资贡献、发起求助、积分扣减。

#### 3.1.6 限流

- 接口级：基于 Redis + Lua 的令牌桶，按用户 + 接口维度。
- AI 类接口（简历优化、模拟舱、对话）单独限流：每用户每分钟 N 次，保护千问配额。

#### 3.1.7 数据权限

| 角色 | 数据范围 |
|------|----------|
| 学生 | 本人所有数据 + 公开职位/帖子/白皮书简版 |
| 校友 | 同学生 + 可发起内推 |
| HR | 本公司发布的职位及对应投递 |
| 导师 | 本人帮扶的帮帮团请求 + 可点评的模拟面试 |
| 管理员 | 薪资审核、用户管理、徽章模板、白皮书发布 |

以 `@DataScope` 注解 + MyBatis 拦截器拼 SQL 条件实现。

#### 3.1.8 审计日志

- 独立 `sys_audit_log` 表（user_id、action、target_type、target_id、ip、ua、detail、time）。
- 切面 `@AuditLog` 标注关键操作：薪资审核、徽章锁定/解锁、积分调整、用户封禁。

#### 3.1.9 多环境配置

`application-{dev,test,prod}.yml`，敏感配置走环境变量；本地用 `docker-compose` 一键起中间件。

#### 3.1.10 单元测试

- Service 层 JUnit5 + Mockito，覆盖率目标核心模块 ≥ 60%。
- API 层 MockMvc 冒烟测试。

### 3.2 模块依赖规范（最终版）

```
iwantjob-api  ──依赖──> 各业务模块
各业务模块    ──依赖──> iwantjob-framework, iwantjob-common
iwantjob-ai   ──定位──> AI 基础设施（仅封装千问调用，不含业务编排）
业务模块间    ──禁止直接互调──> 通过事件/MQ 解耦
积分/用户     ──归属──> iwantjob-user（或独立 iwantjob-points）
```

### 3.3 数据库修订汇总（最终版要点）

> 原文档 SQL 在以下点位修订，其余保持不变。

1. **移除所有跨模块物理外键**（保留索引），改为应用层校验。
2. **所有业务表统一增加**：`is_deleted tinyint default 0`（缺失的补齐）、`updated_at`（缺失的补齐）。
3. `mutual_points` 增加 `version int default 0`（乐观锁）。
4. `simulator_choice`：移除顶层 `next_node_id`，分支信息内嵌进 `options` JSON；或新增 `simulator_node` 表（推荐后者，便于场景可视化编辑）。
5. 新增 `salary_review_log`（薪资审核流转）。
6. 新增 `sys_audit_log`（审计日志）。
7. `help_group_request` 增加 `match_tags varchar(200)`。
8. 枚举值统一抽 `common/enums` 包，DB 注释标注枚举类名。
9. `user_badge` 增加数据库触发器或 Service 层强约束：**仅允许 INSERT，禁止 UPDATE/DELETE**（is_locked 一旦置 1 不可回退）。

### 3.4 三大创新点最终实现方案

#### 3.4.1 薪资透明计划（创新 1）

**脱敏与隐私**：
- 贡献表单不收集姓名、身份证、公司全称；`company_scale` 仅存规模档位（如"100-500人"）。
- `salary_report_data.user_id` 用于积分发放后，对外查询接口**禁止按 user_id 反查明细**。
- `is_anonymous` 默认 1 且不可改为 0。

**审核规则（自动 + 人工）**：
1. 薪资范围合理性：≥ 城市/学历对应最低工资，≤ 行业 P99（需维护一份基准表或用历史数据计算）。
2. 同用户同月同岗位去重（Redis Set 判重）。
3. 超出历史 3σ 的标记为待人工复核。
4. 通过则入 `salary_report_data` 并写 `salary_review_log`。

**权益回馈**：
- 有效贡献 +30 积分（写 `point_transaction`，乐观锁更新余额）。
- 连续 ≥3 次有效贡献或高可信数据，置 `salary_contribution_reward.unlock_match_boost=1`，解锁"精准匹配优先权"。

**白皮书生成**：
- 定时任务（每月 1 日）：聚合 `salary_report_data`（已审核），按城市/岗位/学历/行业分组计算 P25/P50/P75/P99、样本量、同比。
- 调用千问生成分析文字 + 图表数据结构，写入 `salary_whitepaper.report_json`。
- 异步：用 RabbitMQ（或 `@Async`）拆分计算与 AI 生成，避免长事务。

**访问控制**：
- 简版白皮书：公开。
- 高级章节（按学历/双一流对比）：需积分解锁或贡献记录 ≥ 1，写 `unlock_record`。

#### 3.4.2 AI 职业模拟舱（创新 2）

**场景建模**（推荐显式节点图）：
- 新增 `simulator_node`：`id, scenario_id, node_desc, is_end, ai_prompt_snippet`。
- `simulator_node_option`：`id, node_id, option_text, next_node_id, soft_skill_tags`。
- 启动时 AI 也可动态生成后续节点，但结构化存储便于复盘与评分。

**会话流程**：
1. 用户选场景 → 建 `simulator_session`，current_node_id = 场景起始节点。
2. 读当前节点描述 + 选项（Redis 缓存，miss 查 MySQL）。
3. 用户选 → 写 `simulator_choice` → 调千问生成即时反馈 → 更新 current_node_id。
4. 到达 `is_end` 节点 → 调千问基于全程 choices 生成软技能评分（沟通/应变/抗压/协作）→ 写 `overall_score` + 详细反馈 → 触发徽章事件。

**一致性**：每个 choice 立即落 MySQL，Redis 仅缓存当前节点；会话恢复优先读 Redis，miss 从 MySQL 重建。

**Prompt 规范**：系统提示词固定角色设定 + "不得跳出职场场景"约束 + 输出格式（情境描述 + 2~3 选项 JSON），保证分支一致性。

#### 3.4.3 防篡改可信徽章（创新 3）

**措辞定调**：对外表述统一为"**防篡改可信徽章**"——基于数据库约束 + 哈希指纹 + 审计日志实现业务层防伪与可追溯，**不宣称密码学层面不可篡改**（避免答辩被质疑）。

**自动铸造（事件驱动）**：
- 定义 `BadgeTriggerEvent(userId, conditionType, refId)`。
- 触发源：community（分享面经 N 次）、helpgroup（帮扶完成）、simulator（完成场景）、salary（有效贡献 N 次）。
- badge 模块消费事件 → 查 `badge_template` 匹配条件 → 达阈值且未持有 → 铸造。

**锁定与哈希**：
- 铸造时 `is_locked=1`，`lock_hash = SHA256(user_id + badge_id + earned_at + system_salt)`。
- `user_badge` 加 DB 触发器：禁止 UPDATE（除锁定的初始设置外）/ DELETE。
- 写 `badge_lock_log`（action=LOCK, operated_by=null 表示系统）。

**展示与扩展**：
- 个人主页展示已锁定徽章（含 lock_hash 前 8 位作为指纹）。
- 预留企业查验接口（`GET /badges/verify?userId=&badgeId=&hash=`）供未来扩展。

### 3.5 角色权限矩阵（最终版节选）

| 接口 | 学生 | 校友 | HR | 导师 | 管理员 |
|------|:----:|:----:|:--:|:----:|:------:|
| /auth/* | ✅ | ✅ | ✅ | ✅ | ✅ |
| /jobs (发布) | ❌ | ✅(内推) | ✅ | ❌ | ✅ |
| /jobs/apply | ✅ | ✅ | ❌ | ❌ | ❌ |
| /salary/contribute | ✅ | ✅ | ❌ | ❌ | ❌ |
| /salary/whitepaper/latest | 简版 | 简版 | 简版 | 简版 | 全量 |
| /admin/salary/review | ❌ | ❌ | ❌ | ❌ | ✅ |
| /simulator/* | ✅ | ✅ | ❌ | ✅(点评) | ✅ |
| /admin/badges/lock | ❌ | ❌ | ❌ | ❌ | ✅ |
| /help-group/request | ✅ | ✅ | ❌ | ❌ | ❌ |
| /help-group/match | ❌ | ✅ | ❌ | ✅ | ✅ |

### 3.6 里程碑最终版（MVP / 完整版双轨，假设 1 人）

| 阶段 | 周期 | 内容（MVP 优先） |
|------|------|------------------|
| 一 | 1w | 骨架 + 用户认证 + MySQL/Redis + 统一规范（响应/异常/日志） |
| 二 | 1w | 职位 CRUD + 投递（MySQL 搜索兜底）+ 文件上传 |
| 三 | 1.5w | AI 简历优化/评分/匹配 + 模拟面试最小闭环 |
| 四 | 1w | 社区问答 + 积分（乐观锁）+ **薪资白皮书最小闭环**（贡献+审核+简版报告） |
| 五 | 1w | **AI 模拟舱**（入职 + 冲突两场景）+ 帮帮团 |
| 六 | 1w | **防篡改徽章** + 事件驱动打通 |
| 七 | 1w | 完整版扩展：ES 搜索、RabbitMQ 异步、内推、技能交换、众筹、白皮书高级章节 |
| 八 | 1w | 集成测试、Knife4j 文档、压测、毕设论文素材整理 |
| 九 | 0.5w | 缓冲 + 答辩准备 |

> 创新点全部落在 MVP 内（第四~六周），保证答辩有完整演示；ES/MQ 等重型组件放第七周作为增强。

### 3.7 风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| 千问 API 配额/费用 | 高 | 严格限流 + 本地 Mock 开发 + 缓存相似 prompt 结果 |
| 中间件部署运维 | 中 | 分级引入，MVP 只用 MySQL+Redis |
| 9 周工期紧张 | 高 | 双轨制，保 MVP；创新点优先演示 |
| "不可篡改"被质疑 | 中 | 措辞改为"防篡改可信"+ 哈希指纹 + 审计日志 |
| 薪资数据造假 | 中 | 自动审核规则 + 异常值人工复核 + 去重 |
| ES 双写不一致 | 低 | MQ 异步 + 定时对账 |

---

## 四、审批结论

**结论：有条件通过。**

需完成以下**必要修订**后方可进入开发：
1. 移除跨模块物理外键，补齐 `is_deleted`/`updated_at`/`version` 等规范字段（D1/D2/D7）。
2. 修正"不可篡改"措辞为"防篡改可信"，并落实哈希 + 触发器 + 审计（B1/D5/3.4.3）。
3. 补齐统一工程规范章节（3.1）并落地到 common/framework。
4. 明确徽章事件驱动触发、积分乐观锁、ES 双写策略（B2/B4/B5）。
5. 采用 MVP/完整版双轨里程碑，创新点前置（3.6）。
6. 补充管理后台、文件上传、刷新令牌等缺失 API（2.4）。

**可选优化**（建议但非阻塞）：
- 新增 `simulator_node` 表显式建模分支图。
- 新增 `salary_review_log` 审核流转表。
- 角色权限矩阵细化到接口级（3.5 已给出节选）。

完成必要修订后，本文档（原文档 + 本审批补充）即作为《我要工作》平台**最终版设计方案**，可直接指导开发与毕设实施。

---

*审批完成。本报告与原文档合并即为最终版设计方案。*

# 2026-08-30 工作日志

## 《我要工作》平台需求审批
- 审批了用户提供《我要工作 — 大学生智能就业与互助成长平台 开发文档（最终版）》。
- 从技术栈/架构/数据库/API/业务逻辑/工程规范/里程碑七个维度评审，结论为"有条件通过"。
- 产出文件：`E:\毕业设计\我要工作-项目需求审批报告与最终设计方案.md`
- 关键修订要点：
  1. 移除跨模块物理外键；补齐 is_deleted/updated_at/version 规范字段。
  2. "不可篡改徽章"措辞修正为"防篡改可信徽章"（哈希+触发器+审计），避免答辩质疑。
  3. 补齐统一工程规范：Result<T>、错误码、全局异常、日志、幂等、限流、数据权限、审计日志、多环境、测试。
  4. 徽章改事件驱动铸造；积分用乐观锁；ES 双写走 MQ 异步+对账。
  5. 技术栈分级引入（MVP 只用 MySQL+Redis），ES/MQ 后置。
  6. 里程碑改 MVP/完整版双轨，三大创新点前置到第 4-6 周。
  7. 补充管理后台、文件上传、刷新令牌等缺失 API；新增 simulator_node、salary_review_log、sys_audit_log 等表建议。
- 项目背景：Spring Boot 3.3 + JDK 17，Maven 多模块（10 业务模块），约 30 张表，三大创新点（薪资白皮书/AI 模拟舱/防篡改徽章）。毕业设计项目。

## 文档优化与编码就绪审查
- 按审批报告全部修订要点合并原文档，产出优化版最终稿 v2.0。
- 产出文件：`E:\毕业设计\我要工作-开发文档（优化版最终稿v2.0）.md`
  - 落实6项必要修订+3项可选优化，变更点以 [v2.0变更] 标注。
  - 含修订版完整建表SQL（移除外键、补规范字段、新增 sys_audit_log/salary_review_log/simulator_node/simulator_node_option 表、user_badge 防篡改触发器、job/post FULLTEXT 兜底）。
  - 补充管理后台API、文件上传、刷新令牌、徽章企业查验接口；新增角色权限矩阵章节、统一工程规范章节、风险对策章节。
  - 里程碑改 MVP/完整版双轨（创新点前置第4-6周）。
- 编码就绪审查：`E:\毕业设计\我要工作-编码就绪审查报告.md`
  - 结论：可以进入编码阶段，无 P0 阻塞项。
  - 2项 P1 首日待办：①确认千问 API key/配额并搭本地 Mock；②确认人力排期基线。
  - 5项 P2 可并行：system_salt 取值、薪资审核基准表、预置模拟舱场景数据、徽章模板初始数据、Knife4j 分组配置。
  - 给出首周编码启动顺序（Day1-5）：骨架→common/framework→user认证→Security+JWT→权限/审计/幂等/限流切面→积分+文件上传→联调。
- 关键决策：技术栈分级引入（MVP 仅 MySQL+Redis），ES/RabbitMQ 第七周增强。

## 第一阶段编码（多智能体并行）
- 启用多智能体模式：team-lead 搭建前置串行基础（Git+骨架+common+framework），3 个子智能体并行开发。
- 产出文件：`E:\毕业设计\AGENT.md`（协作追踪）、`E:\毕业设计\iwantjob-backend\`（全部代码）
- Git：提交 commit b5c516e 到 main（96 文件，6474 行）。开发分支 feat/phase1-foundation-auth。
- 统计：68 个 Java 文件 + 15 个 pom + 5 个 db 脚本。
- 前置串行（team-lead）：
  - T2 Maven 骨架：父pom + common/framework/ai/modules(9业务模块)/api，共15个pom。
  - T3 common：Result/ErrorCode(46错误码,9段位)/PageResult/PageParam/BusinessException + 9个枚举(UserRole/JobType/ApplicationStatus/BadgeCond/Rarity/ScenarioType/Edu/Benefit/PointReason)。
  - T4 framework：GlobalExceptionHandler/TraceIdFilter/MyBatisPlusConfig(分页+乐观锁+自动填充)/RedisConfig/Knife4jConfig/SecurityConfig/JwtUtils/JwtAuthFilter/SecurityUtils/LoginUser/Idempotent+Aspect/RateLimit+Aspect/AuditLog+Aspect。启动类+application.yml+logback-spring.xml。
- 并行子智能体：
  - agent-user（30文件）：entity(5)+mapper(5,含乐观锁SQL)+dto(8)+service(4+4impl)+controller(4)，实现8个API（注册/登录/刷新/登出/用户信息/资料更新/积分查询/导师咨询解锁），BCrypt加密、JWT黑名单登出、幂等控制。
  - agent-ai（7文件）：AiChatService接口/ChatMessage/StreamCallback/AiProperties/AiUserContext(ThreadLocal)/QwenAiChatService(千问SDK+重试3次+Redis限流10次/分)/MockAiChatService(默认启用,按prompt关键词返回模拟响应)。
  - agent-db（5文件）：docker-compose(MySQL/Redis/RabbitMQ/MinIO/ES)+01-schema.sql(35张表,含防篡改触发器/FULLTEXT/乐观锁)+02-init-data.sql(徽章5条/场景4条/节点分支2组/题库10条/薪资基准10条)+03-drop.sql+README.md。
- 冲突避免：三子智能体目录互不交叉（user/ai/db），零冲突。
- 下一步：第二阶段职位模块+简历模块+文件上传。

## 第二阶段就绪评估（迭代状态报告）
- 产出：`E:\毕业设计\迭代状态报告.md` + 同步更新 `AGENT.md`
- 逐模块核验三个维度：代码规范/接口定义/数据结构设计。
- 结论：第一阶段 5 模块(user/ai/db/common/framework)全部就绪；第二阶段 9 待开发模块中 6 个可直接编码(job/interview/salary/simulator/community/helpgroup)，3 个需补前置(resume待文件上传、badge待事件基类、文件上传本身)。
- 阻塞清单：B1编译验证未执行(P0)、B2文件上传未实现(P1)、B3 BadgeTriggerEvent未定义(P1)、B4千问key(P2,Mock可用)、B6 @DataScope未实现(P2)。无P0硬阻塞(设计缺陷)，B1为验证性动作。
- 任务分配：team-lead串行完成T8编译验证+T11文件上传+T12事件基类后，8个子智能体并行开发第二阶段全部业务模块(目录互不交叉)。
- 质量抽检：AuthServiceImpl(事务/唯一性/BCrypt/JWT黑名单/防重放)、SysUser(@TableLogic/@FieldFill)、01-schema.sql(规范注释/无跨模块外键/触发器)均合格。
